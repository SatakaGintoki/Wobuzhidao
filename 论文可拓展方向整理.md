# A题论文可拓展方向整理

## 1. 使用说明

本文档用于在现有四问基线模型基础上规划论文拓展，不改变当前基线结果，也不把尚未完成的实验写成既定结论。

当前基线已经具备：径向热湿传递方程、变物性、收缩域模型、节点型有限体积离散、隐式BDF时间推进、网格与时间收敛检查，以及完整结果导出。因此后续拓展的重点应从“继续提高小数位精度”转向“解释干燥机制、区分影响因素、量化预测可靠性”。

## 2. 推荐的论文主线

建议把论文组织为以下逻辑：

1. 建立四问统一的热湿传递基线模型；
2. 发现干燥末期表层存在强非线性扩散层；
3. 通过表面加密或改进通量离散提高达标时间的数值可靠性；
4. 通过物性—几何对照解释第三问和第四问的时长差异；
5. 用湿芯演化和全域阈值说明为什么不能只看平均含水率；
6. 通过环境和参数敏感性给出更稳健的工艺判断；
7. 将密度相容性、端面效应和潜热作为适用边界及推广方向。

## 3. 拓展方向总表

| 方向 | 主要贡献类型 | 主要工作量 | 推荐程度 |
|---|---|---:|---|
| 表层强非线性扩散与自适应离散 | 数值方法改进 | 中高 | 最高 |
| 物性与收缩作用分离 | 机制解释 | 中 | 最高 |
| 全域达标与湿芯演化 | 结果分析 | 低 | 很高 |
| 后期环境与参数敏感性 | 稳健性分析 | 中 | 很高 |
| 收缩—密度相容性诊断 | 模型自洽性分析 | 低中 | 推荐，但需修正公式 |
| 轴对称二维端面校核 | 模型扩展 | 高 | 有时间再做 |
| 潜热与界面传质扩展 | 物理推广 | 很高 | 暂不作为主线 |

## 4. 方向一：表层强非线性扩散与自适应离散

### 4.1 研究问题

为什么相同的物理模型在粗网格和表面加密网格下会得到完全不同的干燥时间？

已有诊断显示：第三问中均匀41节点得到约222.7 h，而表面加密后约57.47 h。干燥末期，表面附近含水率很低，扩散系数在约0.5 mm范围内变化约28倍。粗网格跨越这一层时，调和平均会产生过大的数值传质阻力。

### 4.2 可实施方案

- 比较均匀网格、固定表面加密网格和基于扩散系数梯度的自适应网格；
- 根据相邻节点的 `|Delta log D|` 或含水率梯度自动确定加密区域；
- 比较调和平均通量与扩散势积分通量；
- 同时比较达标时间、完整含水率场误差和计算成本。

对于题给结构

\[
D(C,T)=A(T)\exp(-a/C),
\]

可以定义扩散势

\[
\Phi(C)=\int_0^C\exp(-a/s)\,\mathrm ds,
\]

再用势差构造界面通量。实施时仍建议以含水率 `C` 为主要未知量，并与原始调和平均方案交叉验证，避免直接变量变换带来的逆变换误差。

### 4.3 验证要求

- 至少保留G4、G8、G16等网格对照；
- 统一时间容差、后期环境和事件定义；
- 比较同一物理时刻的全场，而不能比较不同保存时刻；
- 不能只展示最终时长，还要展示表层 `D(r)`、通量和质量收支。

### 4.4 论文定位

建议表述为“针对低含水率表层扩散阻力的数值离散改进”，不要直接声称提出了全新的有限体积方法。

## 5. 方向二：物性与收缩作用分离

### 5.1 研究问题

第四问的干燥时间约为51.09 h，第三问约为57.47 h，但两问同时改变了物性公式和半径。需要回答：时长差究竟来自物性、收缩，还是二者的交互作用？

### 5.2 2×2对照实验

| 物性 | 固定半径 | 收缩半径 |
|---|---:|---:|
| 附录3 | 第三问基线 | 新增 |
| 附录4 | 新增 | 第四问基线 |

四组计算应使用相同的输入插值、网格等级、时间容差、后期环境和达标判据。

### 5.3 重点分析

附录4的扩散系数相对附录3为

\[
\frac{D_4}{D_3}=0.175\exp(0.15/C).
\]

两者在约 `C=0.0861` 处反转：高含水率阶段附录4扩散较慢，低含水率阶段附录4扩散反而较快。第四问末期表面含水率约为0.0526，因此不能笼统地说“附录4扩散系数更小”。

建议输出：

- 四组达标时间；
- 中心、表面和平均含水率历程；
- 收缩半径和有效扩散系数历程；
- 物性效应、几何效应和交互效应的定量分解。

### 5.4 论文定位

这是最容易形成完整论证链的机制分析之一，建议作为第四问结果分析的核心拓展。

## 6. 方向三：全域达标与湿芯演化

### 6.1 研究问题

平均含水率达到0.15是否意味着药材各处都达到要求？

现有结果表明，平均含水率大约在36 h左右达到0.15，而全域最大含水率要到51～57 h才达到阈值。平均达标时仍可能有超过一半体积未达标。

### 6.2 建议指标

- 全域最大含水率 `C_max(t)`；
- 中心与表面含水率差；
- 满足 `C>0.15` 的湿芯半径；
- 未达标体积分数；
- 平均达标时间与全域达标时间之差；
- 干燥速率 `-d C_bar/dt` 与平均含水率的关系。

### 6.3 论文价值

该方向不需要改变主求解器，可以直接利用现有结果后处理。它能说明采用分布式热湿模型、采用全域最大值作为结束判据的必要性，也能把四问结果从“表格输出”提升为“干燥阶段解释”。

## 7. 方向四：后期环境与参数敏感性

### 7.1 研究问题

附件1只覆盖前4 h，第三、四问的大部分时间依赖后期环境延续假设。不同延续方式是否会改变干燥时长和结论？

### 7.2 建议情景

- `Ta=50 °C, Ca=0.05` 的名义延续；
- 延续最后一个观测点；
- 延续最后1 h平均值；
- 对传质系数 `hm` 和扩散系数 `D` 做有依据的倍率扰动；
- 对半径插值方式做有限敏感性比较。

### 7.3 输出方式

报告不同情景下的达标时间范围、中心含水率历程和湿芯体积分数。没有统计误差资料时，应称为“情景范围”或“敏感性区间”，不能称为置信区间。

第三问达标附近最大含水率下降速度约为 `0.001049 kg/kg/h`。因此最大含水率的局部偏差会被放大为较明显的时长差异，这说明达标时间应单独进行误差和敏感性分析。

## 8. 方向五：收缩—密度相容性诊断

### 8.1 正确的诊断对象

附件2的半径轨迹已经作为第四问输入，因此不能把它称为独立验证数据。更准确的名称是“基于半径轨迹的模型自洽性诊断”。

若把附录4的 `rho(C)` 解释为真实湿体积密度，并假设干物质守恒、长度不变，则应检验

\[
s_0R_0^2
=
R(t)^2\left[2\int_0^1s(C(\xi,t))\xi\,\mathrm d\xi\right],
\qquad
s(C)=\frac{\rho(C)}{1+C}.
\]

必须对 `s(C)` 做体积加权平均，不能直接用 `s(C_bar)` 代替，因为当前含水率场明显不均匀。

### 8.2 可以得到的结论

在上述附加解释下，可以讨论：

- 半径轨迹与密度公式能否同时满足干物质守恒；
- 各向同性收缩是否与数据相容；
- 为什么最终采用“有效热物性 + 给定径向收缩轨迹”的工程闭合。

### 8.3 表述边界

不能据此宣称题目数据错误，也不能证明真实药材长度一定不变。正确表述应是：在若干附加假设下，各种数据关系不能同时严格成立，因此当前模型是一种可计算的有效近似。

## 9. 方向六：轴对称二维端面校核

### 9.1 研究问题

第一问的短时径向近似较容易成立，但第三、四问持续时间较长，需要判断端面是否影响全域达标时间。

### 9.2 建议方案

建立低分辨率轴对称 `(r,z)` 有限圆柱模型，使用与径向模型一致的物性、边界和初值，比较：

- 一维与二维的中心含水率；
- 全域最大含水率；
- 达标时间；
- 端面通量占总失水量的比例。

### 9.3 当前结论能写到什么程度

用中心历程积分扩散系数并代入 `erfc` 只能作传播尺度的数量级估计，不能直接称为端面误差，也不能单独证明一维结果是严格保守上界。若没有二维对照，论文中应使用“端面影响的量级讨论”这一表述。

## 10. 方向七：潜热与界面传质推广

### 10.1 为什么暂不建议作为主线

直接在现有热方程中增加潜热项还不够。还必须统一：

- 空气含湿量与药材干基含水率的基准；
- 表面平衡含水率或水活度；
- 传质系数的驱动力和单位；
- 同一个水分通量如何同时进入质量方程和能量方程。

现有诊断表明，“只加潜热而不改界面传质边界”可能产生不合理的蒸发方向和过低表面温度。

### 10.2 合适的论文位置

将其放在“模型局限与推广”中，说明需要额外的界面参数和实验数据。除非能够完成界面闭合、参数追溯、数值收敛和独立验证，否则不建议把它作为本届论文主模型。

## 11. 实施优先级

### 第一优先级：建议实际完成

1. 完成物性—收缩2×2对照；
2. 增加湿芯半径、未达标体积分数和干燥速率曲线；
3. 整理表层粗网格失败案例，并进行至少一种针对性离散改进；
4. 做后期环境和关键参数敏感性。

### 第二优先级：根据剩余时间决定

5. 用严格体积加权公式重做收缩—密度相容性诊断；
6. 做低分辨率二维端面校核。

### 第三优先级：作为推广讨论

7. 潜热、吸附等温线和界面水分势耦合；
8. PINN、神经网络或启发式优化。

## 12. 写作和证据纪律

- 代码实现、数值结果、后处理推断和文献支持必须分开标注；
- 未重算的量只能作为待验证假设；
- 附件2已用于驱动第四问时，不能再称其为独立验证数据；
- `erfc` 传播尺度不能写成二维端面误差；
- 不要把“表格四位小数稳定”写成“物理预测精确到四位”；
- 不要把文献中的方法直接称为本题原创方法；
- 不要把“各向同性收缩不相容”扩大为“长度固定已被证明”；
- 论文应优先展示机制解释和可复现对照，而不是堆叠算法名称。

