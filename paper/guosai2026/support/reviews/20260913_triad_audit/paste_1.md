【角色】你是本仓库的第 1 号独立审查员。三个审查员做**完全相同的全文工作**，不是分工。不要读取其他审查员的报告。不要改任何文件。不要重跑长时间 PDE。
【仓库】C:\Users\chens\Desktop\国赛文件夹 GPT
【只读】正式论文 `paper/guosai2026/` ；开发代码 `code/` ；支撑代码 `paper/guosai2026/support/code/` ；结果 `results/` ；根目录 `figures/`
【禁止】`ARCHIVE_legacy/`、`tmp/`、`论文交付_*`、历史 PDF、直接修改 tex/py
【语言】中文；摘录保持原文；符号保持 LaTeX
【输出】严格按文末「报告结构」输出一份完整 Markdown。缺任何一张规定的表，视为未完成。

你是参赛队请来的外部审查员，不是作者，也不是润色编辑。你必须独自完成下面全部事项，不得以「这部分留给别人」跳过：

1. 论文论述是否准确，有无过强表述。
2. 上下文是否冲突（摘要、假设、各问、检验、评价）。
3. 是否有多余重复描述。
4. 符号是否前后一致、是否用错。
5. 论文每一步模型建立是否有对应代码。
6. 论文每个关键结果是否能追溯到 NPZ / JSON / CSV / TeX。
7. 论文每一张图、每一张表是否有对应生成代码；代码生成但未入文的产物也要入账。

先建账本，再下判断。没有填完规定表格，不允许写「整体没有问题」。每条问题必须带：路径、定位（章节 / 公式号 / 函数名）、原文或代码摘录、冲突对象、严重度、建议。禁止编造公式、数值、文件名、哈希、数组键。不确定标 `NEEDS_HUMAN`，并写还缺什么。已有 `support/reports/` 只作线索，必须回源复核。`qa_guosai_paper.py` 等核验脚本只能证明它们检查过的部分。

「程序能跑」不等于「论文这句话来自该程序」。必须指出由哪个数组、哪个键、哪次舍入得到论文中的那个数。若 `code/` 与 `paper/guosai2026/support/code/` 同名文件不同，两边路径都写，不要擅自选边。中文国赛论文只查科学对错和自洽，不按 CS 会议八股打分。

## 唯一正式源

- 论文：`paper/guosai2026/`（阅读版 `reading.tex`，完整版 `main.tex`）
- 开发代码：`code/`
- 支撑材料代码：`paper/guosai2026/support/code/`
- 生产结果：`results/`
- 论文用图：`paper/guosai2026/figures/`
- 论文用表：`paper/guosai2026/tables/`
- 题定表与哈希：`paper/guosai2026/qa/table_sources.json`

排除（除非要指出正式稿误引历史稿）：`ARCHIVE_legacy/`、`tmp/`、`论文交付_*`、`paper/guosai2026_axisymmetric_review/`、旧 PDF。

`main.tex` 顺序：abstract → 01_problem → 01_analysis → 02_assumptions → 02_preprocessing → 03_q1 → 04_q2 → 05_q3（末尾 `\input{sections/07_axisymmetric}`）→ 06_q4 → 07_validation → 08_evaluation → 09_declaration → references；完整版另含 appendix 与 q1_reference_appendix。

## 严重度

| 级别 | 含义 |
|---|---|
| `BLOCKER` | 正式结论、题定表、或关键方程与代码/数据冲突 |
| `MAJOR` | 评委易抓，或溯源断裂（图无生成脚本、同一量两个定义） |
| `MINOR` | 不改结论但应改（重复段、孤儿图、单位漏写） |
| `NOTE` | 不是错误，记录口径或风险 |

状态只用：`OPEN` / `NEEDS_HUMAN` / `PASS`。  
溯源只用：`TRACED`（正文 → 公式或程序 → 结果键 → 正文数字全闭合）/ `PARTIAL` / `UNTRACED` / `ORPHAN`。

## 高压清单（12 条必须逐条给 PASS 或 OPEN，禁止跳过）

1. 问题三 57.4731 h、未舍入最大含水率约 0.1499993913，与表 `tab_q3C` 末行四位小数 0.1500 的关系是否写清，会不会被读成未达标；数字能否从 `q3_solution.npz` 复述。
2. 问题四 51.0877 h、终时半径 1.2000 cm、0.1499989333；固定对照 129.8448 h、节省 78.7571 h / 约 60.65%。是否真同用附录4物性，能否对上 `q4_solution.npz` 与 `q4_fixed_comparison`。
3. 4 h 后 \(T_a=50^\circ\mathrm C\)、\(C_a=0.05\) kg/kg：摘要、假设（6）、第三问、第四问、敏感性表是否同一口径；代码里 `Ca` 扰动是否只改 4 h 后，`h_m` 是否全程缩放。
4. 二维校核「约 8 s / 0.004%」出自哪个 JSON/NPZ；是否只用第三问初值与物性；正文有没有把它写成第四问误差界。
5. 热方程不显式含潜热、水分携焓：假设、问题一、评价、摘要是否一致，有无写成「已证明可忽略」；代码残差是否真的不含这些项。
6. 经验 \(\rho(C)\) 与收缩域干物质密度 \(s\) 在论文和代码里是否分开记账，有无被当成同一个量。
7. \(C_a\) 是烘房题给水分量，还是被写成药材平衡含水率；边界实现是否为题给 Robin 闭合。
8. 材料坐标变换后，论文与代码是否都避免给干基含水率错误添加体积压缩项，并保留 \(R^{-2}\) 与 \(R^{-1}\)。
9. 符号表 \(B,D,s,v,\xi,U,\Theta,W_i,w_i,M,t_*,t_{\mathrm{rep}}\) 后文是否改义或混用；权重与 BDF、事件函数是否与公式同构。
10. 正文每一张图、每一张表是否有生成代码；题定六表是否由同一未舍入解生成（对照 `qa_guosai_paper.py` 逻辑，但必须独立叙述）。
11. `code/` 与 `support/code/` 是否同步；附录 `\VerbatimInput` 是否漏收后加脚本。
12. 四问是否都从题给初值独立起算，有无把上一问末态接到下一问。

## 必做步骤（按这个顺序，不要只做其中一块）

### I. 论述、冲突、冗余、符号

通读摘要到评价；附录程序可略读，附录文字说明要读。建立主张账本、符号账本、冲突对、冗余表。符号至少覆盖 \(t,r,R,R(t),L,T,T_a,T_K,C,C_a,C_s,\rho,c_p,k,B,D,h,h_m,s,v,\xi,U,\Theta,W_i,w_i,M,t_*,t_{\mathrm{rep}},\varepsilon_C\)；表外符号补进表。检查过强表述：数值校核写成实验验证、假设写成已证实、敏感性写成稳健性证明。

### II. 模型 ↔ 代码 ↔ 数字

按问拆链，不要按文件名拆：

- 预处理：附件1/2 → 插值 → \(T_a(t),C_a(t),R(t)\) → 4 h 后延续
- 问题一：环层守恒 → 常物性热 + 非线性水分 → Robin/对称 → FVM+BDF → Bessel–Duhamel → 题定表
- 问题二：附录3物性 → 双向耦合 → 核验 → 题定表
- 问题三：事件 \(M(t)-0.15\) → 临界根 vs 报告时刻 → \(\varepsilon_C\) → 敏感性 → 二维校核
- 问题四：比例收缩 → 材料坐标 → 附录4 → 收缩时长 → 固定半径对照

每个 `\label{eq_*}` 都要入表 5：tex 路径、代码文件与函数、是否同构。无代码则 `UNTRACED`。

每个关键数字写链：`正文数字 ← TeX/JSON ← NPZ键或CSV列 ← 脚本函数 ← 方程编号`。至少包括：摘要问题一 1800 s 四个温湿数、问题二 3 h 中心温湿、问题三时长与 \(\varepsilon_C\)、问题四时长与对照、二维约 8 s、敏感性表各行、检验章各表数字。默认只读已有 `results/q{1,2,3,4}_solution.npz`、`*_validation.json`、`results/q3_axisymmetric/`、`results/q4_fixed_comparison/`、`results/diagnostics/`。缺失则 `NEEDS_HUMAN`。

区分生产路径、核验路径、死路径。表 7 对照 `support/README.md` 与 `sections/appendix.tex` 中的复现命令。

### III. 图和表

搜索全部 `includegraphics`、`\figone`、`\figpair`，零遗漏入表 9。枚举 `tables/*.tex` 与章节内全部 caption 表，入表 10。扫描 `figures/` 与 `paper/guosai2026/figures/` 做孤儿表。caption 与图真值不符（坐标系、时刻、把 72 h 写成烘干时长等）记问题。题定表至少抽查每张首行、末行、含 `---` 的格。

下列只是候选生成脚本，以代码里实际 `savefig` 为准：

| 图像文件 | 候选脚本 |
|---|---|
| `q1_oven_input.pdf` | `code/problem1.py` |
| `q1_T_profiles.pdf` `q1_C_profiles.pdf` | `code/problem1.py` |
| `q2_T_profiles.pdf` `q2_C_profiles.pdf` | `code/problem2.py` |
| `q3_C_profiles.pdf` | `code/plot_q3_results.py`（可能覆盖 `problem3.py`） |
| `q3_axisymmetric_check.pdf` | `code/plot_q3_axisymmetric.py` |
| `q4_R_history.pdf` | `code/plot_q4_radius.py` |
| `q4_C_profiles.pdf` | `code/verify_q4_results.py` |
| `q4_fixed_vs_shrink.pdf` | `code/plot_q4_fixed_comparison.py` |

代码可能还会写出 `q3_C_max_history`、`q3_event_zoom`、`q4_C_history`、中心/表面历程图等。未入正文则为孤儿；若正文或附录声称「见某图」却未引用，升级为 `MAJOR`。

## 问题条目模板

ID 用 `R1-ARG-001`、`R1-SYM-001`、`R1-EQ-001`、`R1-NUM-001`、`R1-FIG-001`、`R1-TAB-001`、`R1-CODE-001` 连续编号。`1` 换成你的审查员编号。

```markdown
### <ID>
- 严重度：
- 状态：
- 溯源：
- 位置：
- 原文/代码摘录：
- 冲突对象：
- 为什么是问题：
- 建议：
```

## 报告结构（一级标题不许改）

```text
# 全文审查 · 审查员1
## 0. 元信息
## 1. 覆盖范围
## 2. 论述、冲突、冗余、符号
## 3. 模型–代码对照
## 4. 关键数字溯源
## 5. 图表与表格溯源
## 6. 问题清单（按严重度）
## 7. 高压清单逐条裁决
## 8. 无法核实与自我限制
## 9. 给汇总用的 15 条优先项
```

### 第 0 节

- 审查员编号、你的模型名、日期
- 实际打开的文件列表
- 没有读到的关键文件
- 写明：未重跑 PDE，数值比对仅基于已有产物（若如此）

### 第 2 节必须含

**表 1 主张账本**

| ID | 主张（一句） | 首次出现 | 再次出现 | 后文是否改口 | 状态 |

**表 2 符号账本**

| 符号 | 符号表定义 | 首次方程/文句 | 后文是否改义 | 单位是否稳定 | 代码对应名 | 状态 |

**表 3 上下文冲突**

| ID | 位置甲 | 位置乙 | 冲突内容 | 严重度 |

**表 4 冗余**

| 位置 | 重复了哪一段意思 | 是否有新信息 | 建议删/留 |

### 第 3 节必须含

**表 5 方程–代码对照**（全部 `eq_*`）

| 公式 label | 论文要点 | 代码路径:符号 | 是否同构 | 溯源 | 备注 |

**表 7 程序入口地图**

| 入口命令 | 写出哪些正式产物 | 是否被附录复现顺序收录 | 状态 |

**表 8 双树差异**

| 相对路径 | `code/` | `support/code/` | 是否相同 |

### 第 4 节必须含

**表 6 关键数字溯源**

| 数字 | 论文位置 | 结果文件与键 | 生成脚本 | 舍入规则 | 溯源 |

### 第 5 节必须含

**表 9 正文图账本**

| 文件名 | label | 所在 tex | caption 要点 | 生成脚本 | 数据源 | 是否在 paper/figures | 溯源 |

**表 10 正文表账本**

| 表 label / 文件 | 所在 tex | 数字来源 | 生成或写入方式 | 抽查结果 | 溯源 |

**表 11 孤儿产物**

| 文件 | 生成脚本 | 论文是否引用 | 附录/README 是否声称 | 处理建议 |

**表 12 声称有、实际无**

| 正文或附录的声称 | 实际文件 | 状态 |

### 第 8 节

列出未打开的文件、未核对的数字、可能因长度漏看的章节。禁止写「全部看完」。

### 第 9 节

15 条以内，按你认为汇总时应先看的顺序。可以是问题，也可以是「此项已闭合，证据在表 x」。

现在开始。先列实际打开的文件，再按第 2→3→4→5 节填满所有表，最后给问题清单和高压裁决。不要跳过任何一类工作。
