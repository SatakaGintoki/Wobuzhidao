# 结果 Excel 官方模板格式对齐

日期：2026-09-13。

按用户要求，以 `C:/Users/chens/Desktop/国赛题目/A题/附件/附件3/` 的四份官方模板为文字与格式依据，修正 `results/result1.xlsx` 至 `result4.xlsx`，并同步 `paper/guosai2026/support/results/` 中的四份副本。

## 修正范围

- 恢复模板宋体字号、非加粗文字、水平及垂直居中，按模板设置列宽和行高。
- 第三问工作表名称从“水分浓度”恢复为官方模板的 `Sheet1`。
- 第四问 A1 恢复为“时间\到药材中心的距离”，取消原先额外添加的表头填充和加粗。
- 按模板的数据行、数字表头样式扩展到完整结果范围，不把示意省略号当作实际输出数据。
- 结果数据区保留 `0.0000` 四位小数格式；时间使用常规数值显示，保留最后的非整数秒报告时刻。第四问原有 `B2` 冻结窗格保留。

## 核验

使用表格技能要求的 Artifact Tool 导入、排版、预览和导出；补齐其导出省略的可选工作表范围元数据，供既有读取程序识别行列范围。

重新读取导出的完整文件，逐格比较修改前后数据，不使用容差：677755 个数值全部相等，第四问 23167 个空白全部相等，工作表行列数量不变。分别核对正式原文件及原论文副本。检查六个工作表的字体、字号、对齐方式及结果数值格式，查看各表首屏预览与第四问表面列。

未重新求解模型，未修改原始题目模板、NPZ 源解、CSV 结果或论文正文。旧文件保存于 `ARCHIVE_all/xlsx_before_template_format_*`；具体位置、输入模板和修改前后文件哈希见同名 JSON 核验记录。

## 与历史记录的关系

历史文件验收记录中的 XLSX 哈希属于修改前版本，不改写历史记录。`verify_q3_closeout.py` 中旧工作表名断言、`verify_q4_delivery.py` 使用的旧 A1 表头断言不适用于这次模板对齐后的文件；本次完整数值和格式核对记录为同名 JSON。原求解导出程序未修改，今后重新导出数值文件时，应再应用本次模板格式步骤。
