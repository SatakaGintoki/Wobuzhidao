# DrawIO 图示生成报告

## 2026-09-13 三张示意图加入正文

- 用户明确要求“你先把这几个示意图加入到正文吧”，已授权采用问题一 v4、问题三 v1、问题四 v2 三张图；下方待审说明为历史记录。
- 三份矢量 PDF 已复制到 `paper/guosai2026/figures/`，正文不依赖工作区外部图片路径。
- 问题一：控制方程之后、初边值之前，`fig_q1_model`，图 3，第 9 页，宽 160 mm。
- 问题三：全域判据之后、事件定位算法之前，`fig_q3_event`，图 6，第 19 页，宽 120 mm；正文明确 `M_h` 为相应网格最大含水率。
- 问题四：比例收缩假设之后，`fig_q4_coordinates`，图 9，第 25 页，宽 160 mm；正文解释同一材料点 `P_0` 与 `P(t)`，图注明确构图比例与点位仅为示意。
- 正式 `reading.pdf` 已更新为 35 页，`main.pdf` 为 118 页。交叉引用与编译通过，无 Overfull/Underfull 或未定义引用；三张插图页及阅读版全页缩略图已目视检查。两版前 35 页正文文本完全一致，三张插图页渲染像素一致。
- 检查材料位于 `_tmp/paper_diagrams/`。本轮未改模型、数值、既有结果图，不代表用户已完成全文终审。

## 2026-09-13 问题一图第四版：统一圆柱浅蓝配色

- 用户要求把前面问题一图的圆柱改成问题四图的偏蓝配色；当前对象 `q1-physical-figure-v4`，状态 `PENDING`，v3 为 `REVISION_REQUESTED`。
- 仅修改圆柱轮廓与填充：轮廓为 `#5470DB`，内部与端面填充为 `#96B7FC`，不透明度依次为 22%、16%、20%，与问题四一致。保留红色传热、蓝色排湿、完整端面和单行公式。
- 同名 Draw.io、PDF、PNG 及生成来源已同步更新。Draw.io XML 解析、唯一 ID 检查和 PDF 编译通过；渲染预览已目视检查，无可见重叠或裁切。PDF 沿用同源 XeLaTeX 导出。
- 本轮为外观修改，模型与数值不变；仍供用户审核，未插入正文，其他图未改。

## 2026-09-13 问题四图第二版：水平轴与内部材料点

- 当前对象：`q4-coordinate-figure-v2`，状态 `PENDING`；用户质疑倾斜圆柱及 `P_0` 的位置，v1 为 `REVISION_REQUESTED`。下方 v1 节保留此前设计与交付记录。
- 核对结论：v1 的径向比例正确，但点选在前端面，不适合表示典型内部材料点；轴线倾角只属构图，没有模型含义。
- 修订：两根圆柱改为水平，半径和径向坐标竖直；`P_0`、`P(t)` 均置于内部中截面，轴向位置保持在 `L/2`。文字紧邻点位，不再通过外置引线指向端面；`r_0` 与 `r(t)` 均从同一截面的轴线量起。
- 示意点取 `r_0/R_0=r(t)/R(t)=0.60`，并非题给位置；右侧外半径仍取左侧的 0.70 作为示意。两点均满足内部轴向位置与 `0<r<R`，没有新增物理或数值结论。
- 同时把表面因子的引线终点改为从当前圆柱几何自动计算，确保水平布局下仍准确落在外表面；内部因子文字移至轴线下方，避免遮挡点位。
- 验证：两轴水平、两点轴向位置相同、径向比例一致、表面引线位于外边界的检查均通过；11 个矢量形状解码和 XML 唯一 ID 检查通过。当前共 54 个可编辑对象，PDF 已重新渲染目视检查，无可见重叠或裁切。
- 同名 Draw.io、PDF、PNG 和生成来源已更新为 v2；仍供用户审核，正文、前两张图和模型数值保持。

## 2026-09-13 问题四收缩与材料坐标示意图

- 审核对象：`q4-coordinate-figure-v1`，状态 `PENDING`。用户要求继续制作第三张图，左右展示初始与收缩状态，中心给出比例速度，表面及内部分别标注 `R^-1`、`R^-2`。
- 产物：`figures/fig_q4_coordinates.drawio`、`figures/fig_q4_coordinates.pdf`、`figures/fig_q4_coordinates.png`。生成来源：`figures/source/build_q4_coordinates.py` 及同源生成的 `figures/source/fig_q4_coordinates.tex`。
- 依据：`paper/guosai2026/sections/06_q4.tex` 的比例收缩假设、速度、材料坐标及定解问题；模型解释参照 `reports/models/q4-model-v1.md`。
- 几何：两根等长完整圆柱采用同一投影，径向尺度统一收缩；端面完整闭合，透明纵剖面与中心轴保留内部结构。灰色长度线与轴线平行，两侧均标明 `L=0.25 m` 不变。
- 对应关系：橙色 `P_0` 与 `P(t)` 是同一材料点，径向箭头分别表示 `r_0` 和 `r(t)`。中间给出 `v(r,t)=R'(t)r/R(t)`、`r(t)=R(t)r_0/R_0`，下方说明 `xi=r_0/R_0=r(t)/R(t)` 不变。比例收缩是模型假设，未写成观测事实。
- 两类尺度因子：表面引线指向圆柱外表面，标 `R(t)^-1` 梯度因子；圆柱内部标 `R(t)^-2` 扩散因子。底部链式法则与径向变系数扩散算子的完整变换对应正文。这里 `D` 按坐标变换取同一局部系数，热方程用 `k` 同理；没有把几何倍率解释为烘干时长倍率。
- 构图比例说明：右侧半径为左侧 0.70 倍，点位置为两侧半径的 0.60 倍，仅用于技术示意，没有取某一实测时刻；不是新模拟或新增定量结论。真实长径比未按物理尺寸绘制。图注需明确几何为示意。

### 导出与检查

- Draw.io 含 56 个可编辑对象，其中 11 个为原生 mxGraph 自定义矢量形状，其他为独立文本、标注与连线；没有用整张位图替代可编辑图形。
- 已核对 Draw.io 官方 `js/grapheditor/Graph.js` 中的 `shape=stencil(...)` 编码方式。11 个形状的 Base64、Raw Deflate、URL 解码和内部 XML 解析全部通过；顶层 XML 与 ID 唯一性检查通过。
- 两侧材料点的距离/半径比独立计算一致；两侧轴向长度相同，径向与端面投影均按 0.70 统一缩小。
- PDF 使用同一几何定义经 XeLaTeX 导出，非 Draw.io 原生导出；单页 552.33 × 322.19 pt，所有字体嵌入。未声称编辑器内视觉验收。
- PDF 渲染为 2000 px 宽预览后检查，去掉干扰内部文字的辅助环线，移开与端面轮廓相交的半径标签，并调整长度尺寸线方向。最终公式完整，文字、端面和引线无可见遮挡或裁切。
- 未修改前两张图、论文正文或四问数值，也未运行新模型计算。

### 放置与图注建议

建议放在 `06_q4.tex` 的“问题分析与比例收缩假设”末尾，在速度公式与比例收缩说明之后、首个 `FloatBarrier` 之前。仍按会话要求先给用户看图。

> 比例径向收缩下的材料坐标示意。长度保持不变，材料点的物理半径随外半径同比缩小，相对位置 `xi=r/R(t)` 保持不变。坐标变换使表面梯度与内部扩散分别出现 `R(t)^-1` 和 `R(t)^-2` 几何因子。图中收缩比例仅作示意，不对应特定实测时刻，长径比不按实际比例绘制。

## 2026-09-13 问题三全域达标事件流程图

- 审核对象：`q3-event-figure-v1`，状态 `PENDING`。用户要求评估是否需要，并按提供的事件流程和蓝橙整体配色绘制。
- 文件：`figures/fig_flow_q3.drawio`、`figures/fig_flow_q3.pdf`、`figures/fig_flow_q3.png`。生成来源：`figures/source/build_q3_event.py` 与同源生成的 `figures/source/fig_flow_q3.tex`。
- 依据：`paper/guosai2026/sections/05_q3.tex` 的全域判据、数值求解与事件定位、数值可靠性段落；模型解释参照 `reports/models/q3.md`。
- 用途：解释全节点最大含水率监测、向下阈值穿越、临界根与严格达标报告时刻的区别，避免把输出采样间隔或四位小数显示作为事件精度和达标判据。
- 主流程：环境与原初值输入 → 联合积分温湿场 → 计算全部内部网格节点的最大含水率 → 检查由 `>0.15` 到 `<=0.15` 的向下穿越 → 局部精算和求根 → 选取严格达标报告时刻 → 网格、时间与收支复核。
- “否”分支回到联合积分，继续当前状态，不重置初值。图中分别标注 `M_h(t_*)=0.15` 与 `t_rep>t_*`、`M_h(t_rep)<0.15`，复核采用同一报告时刻和未舍入值。
- 图示概括已实现流程；经验裕量、整秒及四位小时向上选择的具体规则留在正文，不把经验裕量包装为严格误差界。该图不承诺连续三维材料全域已被证明达标。
- 蓝色表示求解及复核，橙红突出事件判断和严格达标时刻，灰色用于输入及继续积分；无装饰性渐变、阴影或总标题。

### 导出与检查

- 8 个流程节点、8 条绑定源/目标节点的连线、2 个分支标签；另有 2 个根对象。XML 解析成功，所有 ID 唯一，连线端点无缺失，回路目标为联合积分，数学排版已启用。
- 环境没有 Draw.io 桌面导出命令，PDF 由与图源同一几何和文字定义生成的 XeLaTeX 文件导出，未声称 Draw.io 原生导出或编辑器内视觉验收。
- PDF 单页，372.60 × 394.52 pt，全部字体嵌入；已渲染为 1606 × 1700 px 预览检查。
- 菱形内文字及公式完整，公式无拆行；箭头不穿过节点，“是/否”标签明确，回路没有交叉；节点和文字没有可见重叠或裁切。
- 原生宽度约 131.4 mm，正文建议约 120 mm，实际插入后再检查段落分页和字号。
- 本轮未重跑第三问模型，未修改第一张图，也未嵌入或编译论文正文。

### 放置与图注建议

建议放在 `05_q3.tex` 的“全域达标判据的建立”末尾、“数值求解与达标事件定位”之前。先按既有会话要求供用户审核。

> 问题三全域达标事件定位流程。以全部内部网格节点的最大含水率监测向下阈值穿越，局部精算并求得临界时刻后，另取严格低于阈值的报告时刻。网格加密、时间收紧及收支检查用于复核数值可靠性，达标判断使用同一报告时刻的未舍入结果。

## 2026-09-13 问题一物理模型图第三版

- 当前审核对象：`q1-physical-figure-v3`，状态 `PENDING`。用户指出圆柱仍不完整、公式不应换行，v2 因此为 `REVISION_REQUESTED`。下方 v1/v2 段落为各版交付时的历史记录。
- 文件名保持 `figures/fig_q1_model.drawio`、`figures/fig_q1_model.pdf`、`figures/fig_q1_model.png`，已更新为 v3。
- 几何改为完整圆柱的内部纵剖示意：左右两个完整、闭合的实线椭圆端面，上下柱面轮廓连续相连；不再遮去半个端面，不再采用半圆柱外形。径向坐标移到端面轮廓以外的内部区域，避免重叠；仅中心轴保留点划线。
- 两条表面 Robin 公式各占一行，保留通量、梯度和交换项之间的完整等式。公式字号保持约 10.1 pt，右侧区域由 390 增至 468 画布单位，画布宽度由 1120 增至 1180；最小说明字号增至约 9.2 pt，以保持嵌入后可读性。
- 继续采用橙红传热、蓝色排湿；所有公式、边界方向和数值模型含义保持。
- Draw.io 含 36 个可编辑对象，另有 2 个根对象。已重新编译单页 PDF，渲染为 1900 px 宽预览检查；两条边界公式均无换行或裁切，圆柱端面完整，标注未遮挡。
- 当前仍只交付待审图面，未修改论文正文。

## 2026-09-13 问题一物理模型图第二版

- 当前审核对象：`q1-physical-figure-v2`，状态 `PENDING`。用户要求“蓝色和红色换一下”，并质疑圆柱虚线轮廓；v1 为 `REVISION_REQUESTED`。
- 文件名保持 `figures/fig_q1_model.drawio`、`figures/fig_q1_model.pdf`、`figures/fig_q1_model.png`，已更新为 v2。下方初稿节保留 v1 的历史设计与检查记录。
- 配色已在箭头、通量符号、场变量和边界标题中同步互换：橙红 `#E6886D` 表示温度及传热，蓝色 `#5470DB` 表示含水率及排湿。背景改为均匀浅橙，端面为中性灰。
- 几何修正：取消原先与纵剖面叠画的后端虚线椭圆，改为沿轴线剖开的半圆柱。矩形为露出的剖切面，右侧为半圆端面，隐藏部分由不透明剖切面遮挡。全部可见实体轮廓为实线，仅中心轴为点划线。
- 保留前版模型、通量方向及全部边界公式，没有新增物理机制或修改数值结果。
- 生成来源仍为 `figures/source/build_q1_model.py`；Draw.io 含 35 个可编辑图形、连线与文本对象，另有 2 个根对象。XML 解析与唯一 ID 检查完成；单页 PDF 重新编译、渲染并目视检查，未见轮廓穿字、标注重叠或裁切。
- 本轮继续供用户审核，未嵌入正文。

## 2026-09-13 问题一物理模型图初稿

- 审核对象：`q1-physical-figure-v1`，状态 `PENDING`。用户要求“这个开始画吧，我看一下你能画出来什么样子”，本次只交付图示供审阅。
- 文件：`figures/fig_q1_model.drawio`、`figures/fig_q1_model.pdf`、`figures/fig_q1_model.png`。
- 来源：`paper/guosai2026/sections/03_q1.tex` 的控制方程、初始与边界条件；`reports/models/q1.md` 作为模型解释补充，以当前论文章节符号为准。
- 内容：圆柱纵剖面、中心 `r=0`、表面 `r=R`、径向箭头、同域温度场与含水率场、加热向内与失水向外的通量、中心对称条件、表面 Robin 关系。
- 通量口径：`q_s=-k T_r(R,t)` 以向外为正，加热时为负；真实水质量通量 `j_s=-s_0 D(C_s) C_r(R,t)`，图中以 `j_s/s_0` 展示与正文相同的归一化 Robin 条件。没有把 `-D C_r` 直接称为水质量通量。
- 图中未加入实际数值场、数值梯度、汽化潜热或温湿双向耦合；均匀底色不表示材料分层或模拟结果。
- 使用用户建议蓝橙配色，无阴影或装饰性渐变。几何为示意，不按真实长径比绘制。

### 生成与视觉检查

- `figures/source/build_q1_model.py` 从同一组几何与文字定义生成 Draw.io XML 和 `figures/source/fig_q1_model.tex`；两者用于保持本次版本内容同步，不保证后续单独编辑 Draw.io 会自动更新 PDF。
- Draw.io 为 36 个可编辑图形、连线与文本对象，另有 2 个根对象；XML 解析成功、38 个 ID 唯一，启用数学排版。没有把整张图嵌入为位图。
- 环境未发现 Draw.io 桌面命令，本次 PDF 由 XeLaTeX 导出；未声称完成 Draw.io 原生导出或编辑器内视觉验收。
- PDF 为单页，490.96 × 254.25 pt，所有使用字体均嵌入。字号与当前论文页宽相容，建议嵌入宽度不小于 160 mm。
- 已将 PDF 渲染为 1800 px 宽 PNG 检查，并修正中心标注与端面轮廓的拥挤、删除干扰水分公式的横线；最新预览中中文、公式、箭头和边界线无可见遮挡或裁切。
- 本次没有修改论文正文、论文图目录或模型数值，也未重新编译整篇论文。

### 待审核后的放置建议

位置：`03_q1.tex` 的“控制方程的建立”末尾、进入“初始条件与边界条件”之前。

建议图注：

> 圆柱药材的径向热湿传递与边界条件示意。温度场和含水率场定义于同一材料域，中心满足对称条件，外表面满足 Robin 交换条件；箭头表示加热、失水情形下热量传入和水分迁出的方向。通量统一以径向向外为正，水分边界按参考干物质密度归一化。图示不按比例。

### 此前路线图的当前状态

用户认为此前整体路线图外观不合适，并指出范文主要使用局部说明图。`fig_roadmap` 保留为历史草稿，当前不采纳、不嵌入；下文原生成记录与放置建议仅保留作历史参考。

## 图示清单

| 文件 | 类型 | 来源依据 | 用途 | 状态 |
|---|---|---|---|---|
| `figures/fig_roadmap.drawio` | 四问递进技术路线图 | `reports/ANALYSIS_MODELING_REPORT.md`、`reports/STATE.md`、`paper/guosai2026/main.tex` | 展示统一输入、问题一至问题四、统一数值验证与论文输出之间的依赖 | 已生成 |
| `figures/fig_roadmap.pdf` | 上述路线图的论文矢量版 | 与 DrawIO 图源同步的布局 | 后续由 `5writing` 按章节需要嵌入论文 | 已生成 |

## 图示内容

图中包含以下真实节点：统一输入（附件1、附件2、附录2--4）、问题一固定域常物性、问题二固定域变物性、问题三全域达标事件、问题四径向收缩与材料坐标、统一数值验证和论文输出。问题节点之间的箭头表示模型递进，不代表将前一问的四舍五入结果直接作为后一问初值；各问仍按模型记录规定从统一初始状态独立计算。

## 导出与自检记录

- DrawIO 源文件为非空 XML，已通过 XML 解析检查。
- 当前环境未发现 DrawIO 命令行程序，因此 PDF 使用与图源同步的 XeLaTeX 矢量复刻版导出；后续可在 diagrams.net 中打开 `.drawio` 继续编辑或重新导出。
- PDF 为单页、横向矢量图，已渲染为 PNG 做视觉检查。
- 节点无明显重叠，四问箭头方向清晰，颜色按输入/模型/事件/验证/输出区分，未使用阴影和装饰性渐变。
- 本图不承载数值结果，未与 `3coding-visual` 的数据图重复。

## 给论文阶段的嵌入建议

建议放在“问题分析”或“数据预处理”之后，作为论文的总览图。推荐 caption：

> 图：四问递进建模路线。统一输入经固定域常物性模型、固定域变物性耦合模型、全域达标事件模型和径向收缩材料坐标模型逐步扩展，所有结果统一经过数值验证后形成论文输出。

当前仅完成图示产物，未修改 `paper/guosai2026/main.tex` 的章节引用；正式嵌入位置由论文写作阶段决定。
