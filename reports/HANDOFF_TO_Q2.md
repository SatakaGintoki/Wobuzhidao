# A题交接：从第一问讨论进入第二问

更新日期：2026-09-11。工作目录：`C:/Users/chens/Desktop/国赛文件夹 GPT`。

**后续验收更新：**用户追加要求验收第一问，技术结论FAIL，见`reports/VERIFY_REPORT.md`。Excel全量格式/来源一致性、温度解析核验和5图视觉检查通过；前100秒水分N640→1280→2560实算确认空间精度未达预拟标准，四位显示变化。第一问不能记为最终通过；主模型与原结果未覆盖。第二问仍不把第一问末态作为输入。

本交接记录本轮对话末尾的实际进展，不代表所有数值结果已验收。用户希望在新对话承接工作并处理问题二。本轮只整理交接，没有启动第二问计算。新任务范围及授权以用户在新对话实际发送的消息为准。

## 1. 当前材料与阅读顺序

先读本文件，再读下列材料；只按需要阅读大型报告，避免从头重做已完成讨论。

1. `paper/drafts/problem1_model_consolidated.md`：第一问最新论文模型正文，共16组公式。此前20组公式版本已修订，不应据历史描述恢复已删公式。
2. `data/A.pdf`与`data/A_text.txt`：A题《药材的烘干问题》，重点重读问题二、附录3和输出要求。
3. `reports/models/q1.md`、`reports/RESULTS_REPORT.md`：第一問实际模型、数值方法和误差限制。
4. `reports/ANALYSIS_MODELING_REPORT.md`、`reports/LITERATURE_REPORT.md`：整体路线与已核实文献。
5. `reports/STATE.md`、`reports/REVIEW_LOG.md`：历史批准范围。某些旧段落仍写“未计算/正在做第一问/下一步完善潜热”，属于历史记录；本文件与最新正文说明对话末尾的进展，不能借此伪造旧结果通过。

本地数学建模skills位于`skills/`：建模时读取`skills/2analysis-modeling/SKILL.md`及其必要共享参考；数值工作再读取`skills/3coding-visual/SKILL.md`。复用已有授权，不重复选题或整体路线。用户要求转入第二问时，先处理第二问自身的方案；第一问结果并不是第二问的数值初值。

## 2. 本轮末尾的模型取舍

- 已选A题，共四问；整体路线A-route-v1与第一问方案q1-plan-v1此前已通过。
- 第一问保留一维径向导热与变D(C)水分扩散、中心对称、对流换热与等效水分交换边界。
- 原模型已用表面通量描述水分离开药材，不是完全不考虑蒸发；它没有在能量方程显式扣除汽化潜热。
- 用户质疑题面未给aw、β，要求权衡是否写入论文。最终建议并已落实到正文：不把含未知aw、β的方程放入正式模型建立，不采用单纯叠加潜热的M1；潜热必要性与参数不足只在模型评价中如实说明。
- 不把“参数未给”当作“潜热物理上很小”的证据。不从香蕉借参数，不直接把题给hm当成不同驱动力基准的系数。
- Ca与hm作为等效边界参数，是声明的经验假设；不可声称空气含湿量与固体干基含水率天然等价。
- 第一问可计算模型与物理完整性需区分。用户要求正文整理不等于批准全部数值精度，不能将有条件的基准预测包装为已实验验证结果。

## 3. 第一问实际产物与不能丢失的限制

- 源代码：`code/problem1.py`、`code/radial_fvm.py`、`code/utils.py`、`code/q1_analytic.py`。
- 结果：`results/q1_solution.npz`、`results/q1_validation.json`、`results/result1.xlsx`、`results/q1_table_temperature.csv`、`results/q1_table_moisture.csv`。
- 方法：节点型有限体积+隐式BDF，N=640；D(C)用调和平均计算界面值；温度以Bessel—Duhamel解核验。
- 1800秒：中心温度约33.5753°C、表面36.7856°C、表面干基含水率约1.5102。
- 温度相对解析解最大差约2.30e-6°C；水分N320与N640全场差最大约0.00133，发生于1秒表面；题给7×5表点差最大8.69e-5。全场四位小数精度尚未得到证明。
- 基线结果在审核记录仍为待审，未宣称所有最终交付条件已满足。第二问从原初值独立计算，不需要把这个结果当成已验证的数值输入。
- 不覆盖第一问的代码、源结果或结果工作簿；共用代码若要修改，先确认不会改变已有复现结果。

潜热评估只需知道结论，不必重复跑：初始湿密度、自由水潜热解释下，30分钟隐含汽化耗热约44.66kJ、原显热增加约4.80kJ。仅加潜热且保持原失水历程，诊断表面约12.41°C；若Ca解释为空气含湿量且p=101325Pa，同刻露点约33.28°C，存在蒸发方向不相容。详见`reports/Q1_MAIN_MODEL_DECISION.md`、`reports/Q1_LATENT_ENERGY_AUDIT.json`、`results/diagnostics/q1_latent_upgrade/audit.json`。这些是条件性诊断，不是新的正式预测。

## 4. 第二问题面关键条件

第二问统一采用附录3，从t=0重算整个模型；不能把第一问0–1800秒结果拼接进来，也不能用第一问1800秒场作为初值。

初始温度28°C，初始干基含水率2.55；几何仍按题目给定R=0.02m、L=0.25m，第二问不引入第四问的R(t)收缩。

附录3：

\[
\rho(C)=650+128C,\qquad
c_p(C)=1450+2736\frac{C}{C+1},
\]
\[
k(C)=0.21+0.38\frac{C}{C+1},\qquad
D(C,T_K)=2.4\times10^{-3}\exp(-0.45/C)\exp(-3850/T_K).
\]

指数中的温度必须为开尔文，T_K=T_摄氏+273.15。单位按题面，重读原PDF以防提取文本把分式误识。

附件1：`data/附件/附件1.xlsx`，241条数据覆盖0–14400秒，间隔60秒。第一问只用了前31条；第二问所需前3小时有实测环境输入可插值，不应先引入4小时后外推。最终输出时间范围需再核对题面与result2模板，题意有“整个过程规律”与“3h表格输出”的层次，不能擅自扩成数日全秒输出。

论文表：3小时内每隔0.5h、距中心0、0.5、1、1.5、2cm的温度与含水率。工作簿按题面与模板保存每1秒、每0.1cm结果，保留四位小数显示。模板`data/附件/附件3/result2.xlsx`；原模板只读，实际结果放`results/`。

## 5. 第二问必须重新论证的内容

1. 热物性依赖C、D依赖C和T，两场需要联合求解；不能沿用第一问“相互独立”的解释。
2. 给出变量物性热方程的蓄热口径。ρcp T_t与∂t(ρcpT)不同；不能因为参考论文用某一种就直接复制。明确题给经验物性被当作有效系数的近似及质量解释。
3. 导热系数与扩散系数均应在通量形式中处理，避免漏项；固定参考干物质密度的假设须与含水率变量及题给ρ(C)区分。
4. h、hm是否沿用附录2须明确题面解释与依据，不静默新增系数；不重新塞入无来源的aw、β。
5. 第一问的端部时间尺度论证只覆盖30分钟；若沿用一维径向近似，应对3小时范围再核对，不把它当整段2–3天保证。
6. Bessel解析解可用于退化为第一问常物性的核验，不能直接当变量物性耦合方程的解析真值。 
7. 温度输出的°C与D中K严格分离；分别做网格、时间和守恒/物理界限检验。

## 6. 文献与环境

主要方法参考：用户全文`C:/Users/chens/Desktop/国赛论文/1.pdf`，da Silva等，2014，Journal of Food Engineering 141:65–73，DOI10.1016/j.jfoodeng.2014.05.010。全文已读，引用已独立核实。其香蕉参数、平衡含水率与收缩经验式不能代替药材参数。其他核实资料见文献报告L02、L08、L10等，不从标题推断未读全文内容。

数值解释器：`D:/python/python.exe`，现有numpy、scipy、openpyxl、matplotlib可用；无需重新安装。Windows PowerShell；命令、路径和输出应避免编码及转义问题。优先复用插值与有限体积设施，但保持第一问的可复现性。

## 7. 新对话的合理起点

先用简短文字确认已读到最后的模型取舍，再专门形成第二问方案：从题面与附录3推导方程、初边值、物性依赖与求解流程，说明与第一问的差别。不要重新询问选A还是C，不重做第一问全部文献讨论，不把先前被舍弃的未知参数模型重新当默认主模型。第二问数值实施与结果采纳按用户在新对话的明确指令及适用工作流推进。
