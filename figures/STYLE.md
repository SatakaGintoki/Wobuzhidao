# 绘图风格

2026-09-13 插入更新：用户已授权采用问题一 v4、问题三 v1、问题四 v2，三图已加入正式正文，插入宽度分别为 160、120、160 mm。下方“待审”描述保留各版本形成时的记录，当前采用状态以本段及 `reports/REVIEW_LOG.md` 为准。其余临时结果图等待用户更换后再评价配色与字体。

状态：2026-09-13 用户指定蓝橙配色作为整篇论文的配色建议；具体布局与字体仍以各图审核为准。既有数据图未批量重绘。

## 用户指定配色（2026-09-13）

- 主蓝 `#5470DB`，主橙红 `#E6886D`。
- 辅助浅蓝 `#96B7FC`，辅助浅橙 `#F1C2AC`。
- 灰 `#B8B8B8`，浅灰 `#E5E5E5`，文字 `#333333`。
- 本次问题一物理图按用户第二轮反馈互换语义色：橙红表示温度及传热，蓝色表示含水率及排湿；语义同时以文字和箭头方向表达。此处只调整指定图，不批量更改其他图。
- `fig_q1_model` 当前为待审修订版 v4。按用户要求与问题四统一圆柱配色：主蓝 `#5470DB` 轮廓、浅蓝 `#96B7FC` 半透明填充，内部纵剖域与左右端面填充不透明度分别为 22%、16%、20%。蓝色填充仅表示药材几何域，不表示数值场分布或热湿分层。
- 按用户第三轮反馈保留完整圆柱：两个完整椭圆端面由上下柱面轮廓连接，内部采用纵剖透视示意；外轮廓用实线，仅中心轴采用点划线。不再用半圆柱外形。
- 热量和水分的每条 Robin 公式均在一行内完整排出，增加公式区域宽度，不以缩小字号解决换行。
- 画布约 182.5 mm × 89.7 mm；中文宋体、数学字体 Computer Modern；主标注约 9.7–11.4 pt，最小说明约 9.2 pt。若嵌入，建议图宽至少 160 mm，并检查正文实际效果。
- PDF 与 Draw.io 由同一几何和文字定义生成；保留 `figures/source/` 中的生成来源。PDF 使用 XeLaTeX 导出，不是 Draw.io 原生导出。

## 问题三事件流程图初稿

- `fig_flow_q3` 为 `q3-event-figure-v1`，待审。沿用主蓝 `#5470DB`、主橙红 `#E6886D`、灰 `#B8B8B8` 与文字 `#333333`。
- 蓝色用于求解、监测和复核过程；橙红用于阈值判断和严格达标时刻；输入与继续积分采用中性灰。颜色语义按流程图对象定义，不改变第一张图的红热蓝湿约定。
- 主流程自上而下，“否”分支从左侧回到联合积分，不回到初始输入；不增加整体标题、渐变或阴影。
- 中文宋体，主文字约 10.6 pt、公式约 10.1 pt、最小说明约 9.7 pt。原生画布约 131.4 mm × 139.2 mm，建议正文宽度约 120 mm，缩放后仍可读。
- 每条公式保持单行。过程框可用独立的说明行；关键判断采用标准菱形。Draw.io 连线绑定源、目标节点，便于后续拖动编辑。

## 问题四收缩与材料坐标初稿

- `fig_q4_coordinates` 当前为 `q4-coordinate-figure-v2`，待审。按用户反馈采用左右两栏水平、等长、不同半径的完整圆柱，端面投影一致，半透明纵剖面承载内部坐标关系。
- `P_0` 与 `P(t)` 选在同一内部中截面，标注紧邻实际点位；不再放在端面，也不把标签移到轮廓外。径向坐标箭头从该截面的中心轴指向材料点。
- 蓝色 `#5470DB` 表示几何轮廓和内部扩散因子；橙红 `#E6886D` 标记对应材料点、径向位置、收缩映射和表面梯度因子；灰色表示轴线及长度尺寸线。
- 保持所有外轮廓完整闭合，不截掉半个端面；仅中心轴为点划线。没有装饰性光照、阴影、渐变或伪造场分布。
- 原生尺寸约 194.9 mm × 113.7 mm，文字约 10.6–12.3 pt，建议正文按至少 160 mm 宽嵌入。每条公式完整单行，含分式不拆行。
- 几何图为示意：右侧各径向长度取左侧的 0.70，轴向长度保持相同；材料点均取相同相对位置 0.60。上述值仅用于构图，不表示某个实测时刻或新计算结果；图注需保留此说明。

## 既有图形记录

- 字体：Microsoft YaHei / SimHei，图内 8–10 pt
- 图内不放总标题；坐标含符号与单位
- 线宽约 1.2，多曲线用颜色区分，必要时线型辅助
- 导出：PDF，`fonttype=42`
- 第1问初稿：`q1_oven_input.pdf`，`q1_T_profiles.pdf`，`q1_C_profiles.pdf`，`q1_T_center_surface.pdf`，`q1_C_center_surface.pdf`
- 第3问初稿：`q3_C_max_history.pdf`，`q3_C_profiles.pdf`，`q3_max_location.pdf`，`q3_event_zoom.pdf`，`q3_T_center_surface.pdf`
- 第4问初稿：q4_C_history.pdf、q4_C_profiles.pdf；沿用中文字体与现有风格，实际物理距离横轴，数据源results/q4_solution.npz，已视觉检查。

## 非数据图初稿

`fig_roadmap.pdf` 为四问递进技术路线图，采用横向流程布局、克制的语义配色和中文衬线字体；对应可编辑源文件为 `fig_roadmap.drawio`。当前状态为初稿建议，尚未由用户确定为全文统一风格。

## 第三问收尾图形增量（2026-09-11）

q3-closeout-v1沿用原字体和配色。剖面图图例置于坐标轴上方、三列排布以免遮线；事件图横轴采用相对临界时刻的秒数，显示临界前90 s附近的保存状态、临界根及严格报告时刻。两条时间标注显示六位小时，保存点间连线仅为视觉引导。复现code/plot_q3_results.py，来源results/diagnostics/q3_delivery/figure_provenance.json。本条为第三问局部修复，不代表用户选择了全文统一新风格。
